import { styled } from "@mui/material";
import Box from "@mui/material/Box";

export const TotalPriceBox = styled(Box)({
  marginBottom: "20px",
  display: "flex",
  justifyContent: "space-between",
});
