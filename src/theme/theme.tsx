import {createTheme} from "@mui/material";

const theme = createTheme({
    palette: {
        mode: 'light',
        primary: {
            main: '#000000',
        },
        secondary: {
            main: '#f8ebe1',
        },
        background: {
            paper: '#ffffff',
        },
    },
});

export default theme;