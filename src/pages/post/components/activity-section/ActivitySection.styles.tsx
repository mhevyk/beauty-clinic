import { styled } from "@mui/material";
import Box from "@mui/material/Box";

export const ActivitySectionWrapper= styled(Box)({
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
  paddingTop: "18px",
});

export const PostStats = styled(Box)({
  display: "flex",
  gap: "22px",
  alignItems: "center",
});
