query GetTreatmentSessionAvailabilities($employeeId: Int!, $startDate: DateTime!, $endDate: DateTime!) {
  getTreatmentSessionAvailability(employeeId: $employeeId, startDate: $startDate, endDate: $endDate)
}