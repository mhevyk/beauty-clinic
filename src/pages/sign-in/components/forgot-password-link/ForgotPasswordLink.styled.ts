import { styled } from "@mui/material";

import AppLink from "@/components/app-link/AppLink";

export const ForgotPasswordLinkStyled = styled(AppLink)({
  display: "block",
  marginTop: "16px",
  textAlign: "right",
  cursor: "pointer",
});
